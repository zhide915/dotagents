# Roadmap

Future work for dotagents. `README.md` covers the present; this file covers
what's planned — each row gets built when its trigger fires, not before.

| Add | When |
|-----|------|
| Codex support (`.codex-plugin/plugin.json` in the plugin + a `.agents/plugins/marketplace.json` catalog) | You adopt Codex |
